[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# DomainAndIpManager

![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![Flask](https://img.shields.io/badge/Flask-2.3%2B-000000?logo=flask&logoColor=white)
![dnspython](https://img.shields.io/badge/dnspython-2.4%2B-2A6DB0)
![Platform](https://img.shields.io/badge/Platform-CLI%20%2B%20GUI-0A7B83)
![Status](https://img.shields.io/badge/Project-Active-2ea44f)
![Data](https://img.shields.io/badge/Data%20Sets-6-orange)

Управление списками доменов/IP для сценариев AI и GFW, выполнение DNS-lookup и экспорт результатов с метками времени. Включает CLI-скрипты и GUI-редактор.

## 🚀 Обзор

DomainAndIpManager — это Python-инструментарий для:
- Поддержки нескольких наборов списков (`ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default`).
- Разрешения `A`-записей доменов и преобразования их в записи `IP/mask`.
- Объединения IP, полученных из доменов, с пользовательскими IP и CIDR-источниками.
- Экспорта детерминированных выходных файлов с метками времени для последующих сетевых/маршрутизационных процессов.

Поддерживаются оба режима:
- CLI-процессы в `code/nslookup*.py` и утилиты сортировки.
- Web GUI на Flask (`code/gui_app.py` + `gui/*`) для редактирования списков и интерактивного запуска lookup.

### Кратко

| Область | Что вы получаете |
|---|---|
| Наборы списков | `ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default` |
| Интерфейсы | CLI-скрипты + Flask GUI |
| Формат вывода | Текстовые снимки с меткой времени + отсортированные TXT/JSON |
| Основной процесс | Редактирование списков → разрешение доменов → объединение пользовательских диапазонов → экспорт |
| Дополнительный помощник | OCR-извлечение YouTube-трафика в `traffics/` |

## 🎬 Демонстрация

![Domain & IP Manager demo](demos/demo.png)

## ✨ Возможности

- Работа с несколькими наборами списков: `ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default`.
- GUI-редактор списков с циклом save/load/run/copy.
- Опциональные переключатели включения для доменов, пользовательских IP и CIDR-блоков.
- Переключение режима вывода: `Domains + IPs` или `IPs only`.
- Отчет о неуспешных lookup в GUI.
- Снимки вывода с меткой времени в `output/`.
- Утилиты для удаления дубликатов и сортировки смешанных входных данных домен/IP в TXT/JSON.
- Опциональный OCR-помощник для трафика в `traffics/` (ориентирован на YouTube).

## 🗂️ Структура проекта

```text
DomainAndIpManager/
├── README.md
├── requirements.txt
├── start_gui.sh
├── code/
│   ├── gui_app.py
│   ├── list_utils.py
│   ├── nslookup.py
│   ├── nslookup_simplified.py
│   ├── nslookup_simplified_gfw.py
│   ├── nslookup_simplified_gfw_w_ai.py
│   ├── nslookup_simplified_gfw_wo_ai.py
│   ├── unique_sort.py
│   └── unique_sort_print.py
├── gui/
│   ├── index.html
│   ├── app.js
│   └── styles.css
├── data/
│   ├── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_domains.txt
│   ├── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_custom_ips.txt
│   ├── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_cidr.txt
│   └── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_mask.txt
├── output/
├── demos/
├── figs/
├── traffics/
└── i18n/
```

## ✅ Требования

- Python `3.10+` (рекомендуется; код использует современный синтаксис типов).
- `pip`.
- Сетевое подключение для DNS-запросов.
- Опционально для OCR-помощника: бинарники `ffmpeg` и `tesseract` должны быть доступны в `PATH`.

## 📦 Установка

```bash
git clone <your-fork-or-this-repo-url>
cd DomainAndIpManager
pip install -r requirements.txt
```

Зависимости:

```bash
pip install -r requirements.txt
```

## 🖥️ Быстрый старт (GUI)

```bash
./start_gui.sh
```

Откройте `http://127.0.0.1:5000`.

Примечания:
- `start_gui.sh` подготавливает `.venv`, устанавливает зависимости при изменении `requirements.txt` и запускает `code/gui_app.py`.
- Также можно запустить напрямую: `python3 code/gui_app.py`.

## 🧭 Использование

### Использование GUI

1. Выберите набор списков (`AI + GFW`, `AI`, `GFW`, `GFW (No AI)`, `Non-GFW (China)`, `Default`).
2. Отредактируйте текстовые области `Domains`, `Custom IPs` и `CIDR`.
3. Установите `Mask` и режим вывода (`Domains + IPs` или `IPs only`).
4. Нажмите `Save`, чтобы сохранить изменения в `data/*.txt`.
5. Нажмите `Run`, чтобы выполнить разрешение и сгенерировать вывод.
6. Нажмите `Copy`, чтобы скопировать текущий вывод.

### Использование CLI

```bash
python3 code/nslookup_simplified.py
python3 code/nslookup_simplified_gfw.py
python3 code/nslookup_simplified_gfw_w_ai.py
python3 code/nslookup_simplified_gfw_wo_ai.py
python3 code/nslookup.py
```

Каждый скрипт выводит результаты в терминал и записывает `output/<script>_YYYYMMDD_HHMMSS.txt`.

### Инструменты сортировки и нормализации

```bash
python3 code/unique_sort.py -i domain_and_ips.txt -o output/domain_and_ips_unique_sorted.json
python3 code/unique_sort_print.py
```

- `unique_sort.py` поддерживает пользовательские флаги input/output и пишет JSON.
- `unique_sort_print.py` выводит отсортированные домены/IP и записывает и TXT, и JSON в `output/`.
- Если `domain_and_ips.txt` отсутствует в корне репозитория, используйте `-i <path>` с `unique_sort.py` или создайте этот файл.

### Опциональный помощник по извлечению трафика

```bash
python3 traffics/extract_youtube_traffic.py \
  --videos "traffics/ScreenRecording_02-03-2026 07-34-48_1.MP4" \
           "traffics/ScreenRecording_02-03-2026 07-36-29_1.MP4"
```

Этот помощник генерирует markdown-отчеты с доменами/IP, полученными через OCR, в `traffics/` и требует внешние инструменты (`ffmpeg`, `tesseract`).

## 🧾 Файлы данных

Списки построчные и хранятся в `data/`:
- `ai_*` для списков только AI
- `gfw_*` для списков GFW
- `ai_gfw_*` для объединенных списков
- `gfw_wo_ai_*` для GFW без AI
- `non_gfw_*` для списков, доступных из Китая (non-GFW)
- `default_*` для legacy/default-списка

Пример:

```text
data/ai_domains.txt
data/ai_custom_ips.txt
data/ai_cidr.txt
data/ai_mask.txt
```

### Матрица наборов списков

| Набор списков | Файл доменов | Файл пользовательских IP | CIDR-файл | Файл mask |
|---|---|---|---|---|
| `ai` | `data/ai_domains.txt` | `data/ai_custom_ips.txt` | `data/ai_cidr.txt` | `data/ai_mask.txt` |
| `gfw` | `data/gfw_domains.txt` | `data/gfw_custom_ips.txt` | `data/gfw_cidr.txt` | `data/gfw_mask.txt` |
| `ai_gfw` | `data/ai_gfw_domains.txt` | `data/ai_gfw_custom_ips.txt` | `data/ai_gfw_cidr.txt` | `data/ai_gfw_mask.txt` |
| `gfw_wo_ai` | `data/gfw_wo_ai_domains.txt` | `data/gfw_wo_ai_custom_ips.txt` | `data/gfw_wo_ai_cidr.txt` | `data/gfw_wo_ai_mask.txt` |
| `non_gfw` | `data/non_gfw_domains.txt` | `data/non_gfw_custom_ips.txt` | `data/non_gfw_cidr.txt` | `data/non_gfw_mask.txt` |
| `default` | `data/default_domains.txt` | `data/default_custom_ips.txt` | `data/default_cidr.txt` | `data/default_mask.txt` |

## ⚙️ Конфигурация

- Одна запись на строку в каждом файле списка.
- Строки, начинающиеся с `#`, считаются комментариями общей логикой загрузки списков и игнорируются при запуске lookup.
- Маски хранятся по наборам списков в `data/<list>_mask.txt`.

Текущее состояние репозитория:
- Все поставляемые файлы mask сейчас содержат `30` (`ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default`).

Сохраненное примечание из более ранних версий README (оставлено для контекста совместимости):
- `*_mask.txt` управляет CIDR-маской (по умолчанию `32`, список `default` использует `24`).
- Уточнение: в текущих закоммиченных данных и значениях по умолчанию в скриптах активные runtime-значения — `30`, если не переопределены.

## 📤 Выходные данные

- GUI + CLI: `output/<script or gui>_YYYYMMDD_HHMMSS.txt`
- Инструменты сортировки: `output/domain_and_ips_unique_sorted.txt` и `.json`

## 🧪 Примеры

Пример запуска CLI:

```bash
python3 code/nslookup_simplified_gfw_w_ai.py
```

Типичная форма вывода:

```text
<domain.example>
<resolved-ip>/30
<custom-ip>/30
<cidr-block>
```

Пример пользовательской нормализации JSON:

```bash
python3 code/unique_sort.py -i ./my_list.txt -o ./output/my_list_unique_sorted.json
```

## 🛠️ Примечания для разработки

- Стиль кода: Python 3, PEP 8, отступы 4 пробела, именование `snake_case`.
- Скрипты намеренно ориентированы на CLI и в основном одноцелевые.
- Несколько вариантов `nslookup` сейчас используют почти идентичную логику с разным маппингом ключей списков.
- В этом репозитории сейчас нет автоматизированных тестов.

## 🧯 Устранение неполадок

- `Input file not found: domain_and_ips.txt`:
  - Передайте `-i <input-file>` в `code/unique_sort.py` или создайте `domain_and_ips.txt` в корне репозитория.
- GUI не открывается автоматически:
  - Откройте `http://127.0.0.1:5000` вручную после запуска.
- Для некоторых доменов пустые DNS-результаты:
  - Проверьте доступность сети/DNS; неразрешенные домены перечисляются в GUI в `Failed Lookups`.
- Отсутствуют зависимости:
  - Выполните `pip install -r requirements.txt`.
- OCR-помощник завершается ошибкой из-за отсутствующей команды:
  - Установите `ffmpeg` и `tesseract` и убедитесь, что обе команды доступны в `PATH`.

## 🗺️ План развития

- Добавить автоматизированные тесты для edge case-ов парсинга, сортировки и lookup.
- Снизить дублирование логики между вариантами `nslookup` через общий параметризованный runner.
- Расширить многоязычную документацию в `i18n/`.
- Добавить опциональные CI-проверки для linting и smoke-тестов.

## 🤝 Contributing

Вклад в проект приветствуется.

Рекомендуемый процесс:
1. Создайте ветку для вашего изменения.
2. Держите коммиты сфокусированными и в повелительной форме (например: `Limit domain list to ChatGPT, Claude, and Google AI`).
3. При изменении поведения генерации данных добавляйте примеры вывода команд.
4. Откройте PR с кратким описанием и заметками о зависимостях/runtime при необходимости.

## 📄 Лицензия

В корне репозитория сейчас нет явного файла `LICENSE`. Если вы планируете распространять или повторно использовать проект, сначала добавьте или подтвердите условия лицензии.

## 💖 Поддержка

Метаданные финансирования также доступны в `.github/FUNDING.yml`.

- GitHub Sponsors: `https://github.com/sponsors/lachlanchen`
- Ссылки проекта: `https://lazying.art`, `https://chat.lazying.art`, `https://onlyideas.art`

### Donation QR (если хотите поддержать напрямую)

| WeChat | Alipay |
|---|---|
| ![WeChat donation QR](figs/donate_wechat.png) | ![Alipay donation QR](figs/donate_alipay.png) |

## 📝 Примечания

- Одна запись на строку в файлах данных.
- `*_mask.txt` управляет CIDR-маской (по умолчанию `32`, список `default` использует `24`).
- Примечание о состоянии i18n: `i18n/` существует в этом репозитории; локализованные README запланированы и должны содержать одну строку выбора языка вверху.
