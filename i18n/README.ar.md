[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# DomainAndIpManager

![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![Flask](https://img.shields.io/badge/Flask-2.3%2B-000000?logo=flask&logoColor=white)
![dnspython](https://img.shields.io/badge/dnspython-2.4%2B-2A6DB0)
![Platform](https://img.shields.io/badge/Platform-CLI%20%2B%20GUI-0A7B83)
![Status](https://img.shields.io/badge/Project-Active-2ea44f)
![Data](https://img.shields.io/badge/Data%20Sets-6-orange)

إدارة قوائم النطاقات/عناوين IP لسياقات الذكاء الاصطناعي وGFW، وتشغيل استعلامات DNS، وتصدير مخرجات مؤرخة زمنيًا. يتضمن أدوات CLI ومحرر GUI.

## 🚀 نظرة عامة

DomainAndIpManager عبارة عن مجموعة أدوات Python من أجل:
- صيانة مجموعات قوائم متعددة (`ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default`).
- حل سجلات `A` للنطاقات وتحويلها إلى إدخالات `IP/mask`.
- دمج عناوين IP المشتقة من النطاقات مع مصادر IP مخصصة وCIDR.
- تصدير ملفات خرج حتمية ومؤرخة زمنيًا لسيناريوهات الشبكات/التوجيه اللاحقة.

وهو يدعم كلا النمطين:
- تدفقات عمل CLI في `code/nslookup*.py` وأدوات الفرز.
- واجهة ويب GUI مبنية على Flask (`code/gui_app.py` + `gui/*`) لتحرير القوائم وتشغيل الاستعلامات بشكل تفاعلي.

### لمحة سريعة

| المجال | ما الذي تحصل عليه |
|---|---|
| مجموعات القوائم | `ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default` |
| الواجهات | سكربتات CLI + واجهة Flask GUI |
| نمط المخرجات | لقطات نصية مؤرخة زمنيًا + TXT/JSON مرتبة |
| سير العمل الأساسي | تحرير القوائم → حل النطاقات → دمج النطاقات المخصصة → تصدير |
| أداة اختيارية | استخراج OCR لحركة YouTube ضمن `traffics/` |

## 🎬 عرض توضيحي

![Domain & IP Manager demo](demos/demo.png)

## ✨ الميزات

- سير عمل متعدد مجموعات القوائم: `ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default`.
- محرر قوائم GUI مع مسار عمل حفظ/تحميل/تشغيل/نسخ.
- عناصر تحكم اختيارية لتضمين النطاقات، وعناوين IP المخصصة، وكتل CIDR.
- مفتاح تبديل وضع المخرجات: `Domains + IPs` أو `IPs only`.
- تقرير عن حالات فشل الحل داخل GUI.
- لقطات مخرجات مؤرخة زمنيًا ضمن `output/`.
- أدوات مساعدة لإزالة التكرارات وفرز مدخلات مختلطة (نطاق/IP) إلى TXT/JSON.
- أداة OCR اختيارية ضمن `traffics/` (موجهة لاستخراج YouTube).

## 🗂️ بنية المشروع

```text
DomainAndIpManager/
├── README.md
├── requirements.txt
├── start_gui.sh
├── code/
│   ├── gui_app.py
│   ├── list_utils.py
│   ├── nslookup.py
│   ├── nslookup_simplified.py
│   ├── nslookup_simplified_gfw.py
│   ├── nslookup_simplified_gfw_w_ai.py
│   ├── nslookup_simplified_gfw_wo_ai.py
│   ├── unique_sort.py
│   └── unique_sort_print.py
├── gui/
│   ├── index.html
│   ├── app.js
│   └── styles.css
├── data/
│   ├── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_domains.txt
│   ├── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_custom_ips.txt
│   ├── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_cidr.txt
│   └── {ai,gfw,ai_gfw,gfw_wo_ai,non_gfw,default}_mask.txt
├── output/
├── demos/
├── figs/
├── traffics/
└── i18n/
```

## ✅ المتطلبات المسبقة

- Python `3.10+` (موصى به؛ الكود يستخدم صياغة أنواع حديثة).
- `pip`.
- اتصال شبكة لتنفيذ استعلامات DNS.
- اختياريًا لأداة OCR: توفر `ffmpeg` و`tesseract` ضمن `PATH`.

## 📦 التثبيت

```bash
git clone <your-fork-or-this-repo-url>
cd DomainAndIpManager
pip install -r requirements.txt
```

الاعتماديات:

```bash
pip install -r requirements.txt
```

## 🖥️ بدء سريع (GUI)

```bash
./start_gui.sh
```

افتح `http://127.0.0.1:5000`.

ملاحظات:
- `start_gui.sh` يقوم بإعداد `.venv`، ويثبت الاعتماديات عند تغيّر `requirements.txt`، ثم يشغل `code/gui_app.py`.
- يمكنك أيضًا التشغيل مباشرة عبر `python3 code/gui_app.py`.

## 🧭 الاستخدام

### استخدام GUI

1. اختر مجموعة قوائم (`AI + GFW`, `AI`, `GFW`, `GFW (No AI)`, `Non-GFW (China)`, `Default`).
2. عدّل حقول `Domains` و`Custom IPs` و`CIDR` النصية.
3. اضبط `Mask` ووضع المخرجات (`Domains + IPs` أو `IPs only`).
4. انقر `Save` لحفظ التغييرات في `data/*.txt`.
5. انقر `Run` لتنفيذ الحل وتوليد المخرجات.
6. انقر `Copy` لنسخ المخرجات الحالية.

### استخدام CLI

```bash
python3 code/nslookup_simplified.py
python3 code/nslookup_simplified_gfw.py
python3 code/nslookup_simplified_gfw_w_ai.py
python3 code/nslookup_simplified_gfw_wo_ai.py
python3 code/nslookup.py
```

كل سكربت يطبع النتائج في الطرفية ويكتب `output/<script>_YYYYMMDD_HHMMSS.txt`.

### أدوات الفرز والتطبيع

```bash
python3 code/unique_sort.py -i domain_and_ips.txt -o output/domain_and_ips_unique_sorted.json
python3 code/unique_sort_print.py
```

- `unique_sort.py` يدعم خيارات إدخال/إخراج مخصصة ويكتب JSON.
- `unique_sort_print.py` يطبع النطاقات/عناوين IP المرتبة ويكتب TXT وJSON داخل `output/`.
- إذا لم يكن `domain_and_ips.txt` موجودًا في جذر المستودع، استخدم `-i <path>` مع `unique_sort.py` أو أنشئ الملف.

### أداة استخراج حركة اختيارية

```bash
python3 traffics/extract_youtube_traffic.py \
  --videos "traffics/ScreenRecording_02-03-2026 07-34-48_1.MP4" \
           "traffics/ScreenRecording_02-03-2026 07-36-29_1.MP4"
```

تقوم هذه الأداة بإنشاء تقارير markdown مشتقة من OCR للنطاقات/عناوين IP داخل `traffics/` وتتطلب أدوات خارجية (`ffmpeg`, `tesseract`).

## 🧾 ملفات البيانات

القوائم مخزنة بصيغة سطر لكل إدخال داخل `data/`:
- `ai_*` لقوائم الذكاء الاصطناعي فقط
- `gfw_*` لقوائم GFW
- `ai_gfw_*` للقوائم المجمعة
- `gfw_wo_ai_*` لقوائم GFW بدون AI
- `non_gfw_*` للقوائم القابلة للوصول من الصين (غير GFW)
- `default_*` للقائمة الافتراضية/القديمة

مثال:

```text
data/ai_domains.txt
data/ai_custom_ips.txt
data/ai_cidr.txt
data/ai_mask.txt
```

### مصفوفة مجموعات القوائم

| List set | Domains file | Custom IPs file | CIDR file | Mask file |
|---|---|---|---|---|
| `ai` | `data/ai_domains.txt` | `data/ai_custom_ips.txt` | `data/ai_cidr.txt` | `data/ai_mask.txt` |
| `gfw` | `data/gfw_domains.txt` | `data/gfw_custom_ips.txt` | `data/gfw_cidr.txt` | `data/gfw_mask.txt` |
| `ai_gfw` | `data/ai_gfw_domains.txt` | `data/ai_gfw_custom_ips.txt` | `data/ai_gfw_cidr.txt` | `data/ai_gfw_mask.txt` |
| `gfw_wo_ai` | `data/gfw_wo_ai_domains.txt` | `data/gfw_wo_ai_custom_ips.txt` | `data/gfw_wo_ai_cidr.txt` | `data/gfw_wo_ai_mask.txt` |
| `non_gfw` | `data/non_gfw_domains.txt` | `data/non_gfw_custom_ips.txt` | `data/non_gfw_cidr.txt` | `data/non_gfw_mask.txt` |
| `default` | `data/default_domains.txt` | `data/default_custom_ips.txt` | `data/default_cidr.txt` | `data/default_mask.txt` |

## ⚙️ الإعدادات

- إدخال واحد في كل سطر ضمن كل ملف قوائم.
- الأسطر التي تبدأ بـ `#` تُعامل كتعليقات بواسطة منطق تحميل القوائم المشترك ويتم تجاهلها أثناء التشغيل.
- يتم تخزين الأقنعة لكل مجموعة قوائم في `data/<list>_mask.txt`.

حالة المستودع الحالية:
- جميع ملفات الأقنعة المضمنة حاليًا تحتوي على `30` (`ai`, `gfw`, `ai_gfw`, `gfw_wo_ai`, `non_gfw`, `default`).

ملاحظة محفوظة من إصدارات README السابقة (لأجل توافق السياق):
- `*_mask.txt` يتحكم في قناع CIDR (الافتراضي هو `32`، وقائمة `default` تستخدم `24`).
- توضيح: في البيانات الحالية المسجلة داخل المستودع وفي القيم الافتراضية للسكربتات، القيم النشطة أثناء التشغيل هي `30` ما لم يتم تجاوزها.

## 📤 المخرجات

- GUI + CLI: `output/<script or gui>_YYYYMMDD_HHMMSS.txt`
- أدوات الفرز: `output/domain_and_ips_unique_sorted.txt` و`.json`

## 🧪 أمثلة

مثال تشغيل CLI:

```bash
python3 code/nslookup_simplified_gfw_w_ai.py
```

شكل مخرجات نموذجي:

```text
<domain.example>
<resolved-ip>/30
<custom-ip>/30
<cidr-block>
```

مثال تطبيع JSON مخصص:

```bash
python3 code/unique_sort.py -i ./my_list.txt -o ./output/my_list_unique_sorted.json
```

## 🛠️ ملاحظات التطوير

- نمط الكود: Python 3، ومعيار PEP 8، وإزاحة 4 مسافات، وتسمية `snake_case`.
- السكربتات مصممة عمدًا لتكون ملائمة لـ CLI ومعظمها أحادي الغرض.
- عدة نسخ من `nslookup` تشارك حاليًا منطقًا شبه متطابق مع اختلاف تعيين مفاتيح القوائم.
- لا توجد اختبارات آلية حاليًا في هذا المستودع.

## 🧯 استكشاف الأخطاء وإصلاحها

- `Input file not found: domain_and_ips.txt`:
  - مرّر `-i <input-file>` إلى `code/unique_sort.py` أو أنشئ `domain_and_ips.txt` في جذر المستودع.
- واجهة GUI لا تفتح تلقائيًا:
  - افتح `http://127.0.0.1:5000` يدويًا بعد التشغيل.
- نتائج DNS فارغة لبعض النطاقات:
  - تحقق من توفر الشبكة/DNS؛ تظهر النطاقات غير المحلولة في `Failed Lookups` داخل GUI.
- اعتماديات مفقودة:
  - شغّل `pip install -r requirements.txt`.
- فشل أداة OCR بسبب أمر مفقود:
  - ثبّت `ffmpeg` و`tesseract` وتأكد أنهما متاحان ضمن `PATH`.

## 🗺️ خارطة الطريق

- إضافة اختبارات آلية لحالات الحافة في التحليل والفرز والاستعلام.
- تقليل المنطق المكرر بين نسخ `nslookup` عبر مُشغّل مشترك قابل للتهيئة.
- توسيع التوثيق متعدد اللغات ضمن `i18n/`.
- إضافة فحوصات CI اختيارية للتنسيق واختبارات smoke.

## 🤝 المساهمة

نرحب بالمساهمات.

سير عمل مقترح:
1. أنشئ فرعًا للتغيير الخاص بك.
2. اجعل كل commit مركزًا وبصيغة فعل أمر (مثلًا: `Limit domain list to ChatGPT, Claude, and Google AI`).
3. أدرج عينات من مخرجات الأوامر عند تغيير سلوك البيانات المولدة.
4. افتح PR مع ملخص موجز وأي ملاحظات عن الاعتماديات/بيئة التشغيل.

## 📄 الترخيص

لا يوجد ملف `LICENSE` صريح حاليًا في جذر المستودع. إذا كنت تخطط لإعادة توزيع المشروع أو إعادة استخدامه، أضف شروط الترخيص أو أكدها أولًا.

## 💖 الدعم

بيانات التمويل متاحة أيضًا في `.github/FUNDING.yml`.

- GitHub Sponsors: `https://github.com/sponsors/lachlanchen`
- روابط المشروع: `https://lazying.art`, `https://chat.lazying.art`, `https://onlyideas.art`

### Donation QR (if you want to support directly)

| WeChat | Alipay |
|---|---|
| ![WeChat donation QR](figs/donate_wechat.png) | ![Alipay donation QR](figs/donate_alipay.png) |

## 📝 ملاحظات

- إدخال واحد في كل سطر في ملفات البيانات.
- `*_mask.txt` يتحكم في قناع CIDR (الافتراضي هو `32`، وقائمة `default` تستخدم `24`).
- ملاحظة حالة i18n: المجلد `i18n/` موجود في هذا المستودع؛ وملفات README المترجمة مخطط لها ويجب أن تحتوي سطر خيارات لغة واحدًا في الأعلى.
